AWSTemplateFormatVersion": "2010-09-09"
Description: A simple s3 Bucket
Resources:
  S3Bucket:
    Type: 'AWS::S3::Bucket'
   # We don't need to supply a Bucket name as AWS will generate a random name for us